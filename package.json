{
  "name": "claude-notebook-indexcol",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install && npm run build",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "openai": "^4.0.0",
    "google-auth-library": "^8.7.0",
    "multer": "^1.4.5-lts.1",
    "@google-cloud/storage": "^6.0.0"
  }
}
